name: Continuity E2E v4

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  render-continuity:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    env:
      APP: https://wae-inteligencia-universal-vt3h.onrender.com
    steps:
      - name: Verify current Render health
        shell: bash
        run: |
          set -euo pipefail
          HEALTH=$(curl -fsS --max-time 30 "$APP/api/health")
          jq -e '.ok == true and .service == "wae-universal-runtime" and .ready == true' <<<"$HEALTH" >/dev/null
          jq '{ok,version,ready,generativeReady,providers,memory,continuity}' <<<"$HEALTH"

      - name: Verify current normal answer path
        shell: bash
        run: |
          set -euo pipefail
          PAYLOAD=$(jq -n '{message:"Responde solamente con la palabra OK.",mode:"general",sessionId:"continuity-e2e-v4-auto",history:[],attachments:[],tools:[]}')
          RESULT=$(curl -fsS --max-time 90 "$APP/api/chat" -H 'content-type: application/json' --data-binary @- <<<"$PAYLOAD")
          jq -e '(.reply // "") | type == "string" and length > 0' <<<"$RESULT" >/dev/null
          jq -e '(.response_schema // .response.schema // "") == "assistant-response/v1"' <<<"$RESULT" >/dev/null
          jq '{provider,model,degraded,latencyMs,fallbackFailures,reply}' <<<"$RESULT"

      - name: Wait for continuity contract
        if: github.ref == 'refs/heads/main'
        shell: bash
        run: |
          set -euo pipefail
          for i in $(seq 1 36); do
            HEALTH=$(curl -fsS --max-time 20 "$APP/api/health" || true)
            if jq -e '.ok == true and .ready == true and (.version | type == "string") and (.version | length) > 0 and .continuity.configured == true and .continuity.provider == "universal_continuity_core" and .continuity.model == "universal-core-continuity-v1" and .continuity.externalProvider == false' <<<"$HEALTH" >/dev/null 2>&1; then
              jq '{version,generativeReady,continuity}' <<<"$HEALTH"
              exit 0
            fi
            sleep 5
          done
          echo 'continuity contract not observed' >&2
          exit 1

      - name: Verify deterministic continuity provider
        if: github.ref == 'refs/heads/main'
        shell: bash
        run: |
          set -euo pipefail
          PAYLOAD=$(jq -n '{message:"Conserva la evidencia del archivo.",mode:"general",provider:"continuity_core",sessionId:"continuity-e2e-v4-core",history:[],attachments:[{name:"estado.txt",type:"text/plain",text:"Estado de prueba: 4200 MXN"}],tools:[]}')
          RESULT=$(curl -fsS --max-time 30 "$APP/api/chat" -H 'content-type: application/json' --data-binary @- <<<"$PAYLOAD")
          jq -e '.provider == "universal_continuity_core" and .model == "universal-core-continuity-v1" and .degraded == true' <<<"$RESULT" >/dev/null
          jq -e '(.reply // "") | contains("4200 MXN")' <<<"$RESULT" >/dev/null
          jq -e '(.response.metadata.degraded // false) == true' <<<"$RESULT" >/dev/null
          jq '{provider,model,degraded,latencyMs,reply}' <<<"$RESULT"
