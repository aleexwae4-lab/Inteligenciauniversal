(()=>{
  const SUPABASE_URL='https://pbswcbryxawsmltyromd.supabase.co';
  const SUPABASE_KEY='sb_publishable_2zXa35U9Z--xuy_mQekG9w_kY7AVlv-';
  const RUNTIME_ENDPOINT=`${SUPABASE_URL}/functions/v1/wae-local-voice-demo-v61`;
  const esc=t=>String(t??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const safeUrl=u=>{try{const x=new URL(String(u));return /^https?:$/.test(x.protocol)?x.href:'#'}catch{return'#'}};
  const sourceMap=sources=>new Map((Array.isArray(sources)?sources:[]).map(x=>[String(x.key||''),x]));

  function inline(raw,sources){let t=esc(raw);t=t.replace(/`([^`]+)`/g,'<code>$1</code>');t=t.replace(/\*\*([^*]+)\*\*/g,'<strong>$1</strong>');t=t.replace(/__([^_]+)__/g,'<strong>$1</strong>');t=t.replace(/(^|[^*])\*([^*\n]+)\*/g,'$1<em>$2</em>');t=t.replace(/\[([^\]]+)\]\((https?:\/\/[^\s)]+)\)/g,(m,label,url)=>`<a href="${esc(safeUrl(url))}" target="_blank" rel="noopener noreferrer">${label}</a>`);const sm=sourceMap(sources);t=t.replace(/\[(W\d+)\]/g,(m,key)=>sm.has(key)?`<a class="iu-source-ref" href="#source-${esc(key)}" title="Ver fuente">${esc(key)}</a>`:m);return t}
  function chart(spec){if(!spec||!['bar','line'].includes(spec.type)||!Array.isArray(spec.items)||!spec.items.length)return'';const items=spec.items.slice(0,20).map(x=>({label:String(x.label||''),value:Number(x.value)})).filter(x=>x.label&&Number.isFinite(x.value));if(!items.length)return'';const min=Math.min(0,...items.map(x=>x.value)),max=Math.max(0,...items.map(x=>x.value)),span=max-min||1;if(spec.type==='line'){const w=640,h=230,p=30,pts=items.map((x,i)=>{const xx=p+(i*(w-p*2)/Math.max(1,items.length-1)),yy=h-p-((x.value-min)/span)*(h-p*2);return[xx,yy]});return `<figure class="iu-chart"><figcaption>${esc(spec.title||'Gráfica')}</figcaption><div class="iu-line-wrap"><svg viewBox="0 0 ${w} ${h}" role="img" aria-label="${esc(spec.title||'Gráfica')}"><path d="M ${pts.map(p=>p.join(' ')).join(' L ')}" fill="none" stroke="currentColor" stroke-width="3"/><g>${pts.map((p,i)=>`<circle cx="${p[0]}" cy="${p[1]}" r="4"><title>${esc(items[i].label)}: ${items[i].value}</title></circle>`).join('')}</g></svg></div><div class="iu-chart-labels">${items.map(x=>`<span>${esc(x.label)}</span>`).join('')}</div></figure>`}return `<figure class="iu-chart"><figcaption>${esc(spec.title||'Comparación')}</figcaption><div class="iu-bars">${items.map(x=>{const pct=Math.max(3,((x.value-min)/span)*100);return`<div class="iu-bar-row"><span>${esc(x.label)}</span><div><i style="width:${pct}%"></i></div><strong>${esc(new Intl.NumberFormat('es-MX',{maximumFractionDigits:2}).format(x.value))}</strong></div>`}).join('')}</div></figure>`}
  function tableData(data,sources){const headers=Array.isArray(data?.headers)?data.headers:[],rows=Array.isArray(data?.rows)?data.rows:[];if(!headers.length)return'';return `<div class="iu-table-wrap"><table><thead><tr>${headers.map(x=>`<th>${inline(x,sources)}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr>${headers.map((_,i)=>`<td>${inline(r?.[i]||'',sources)}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`}
  function component(c,sources){const d=c?.data||{};switch(c?.type){case'heading':{const l=Math.max(2,Math.min(4,Number(d.level||2)+1));return `<h${l}>${inline(d.text||'',sources)}</h${l}>`}case'paragraph':return `<p>${inline(d.text||'',sources)}</p>`;case'quote':return `<blockquote>${inline(d.text||'',sources)}</blockquote>`;case'list':{const tag=d.ordered?'ol':'ul';return `<${tag}>${(d.items||[]).map(x=>`<li>${inline(x,sources)}</li>`).join('')}</${tag}>`}case'table':return tableData(d,sources);case'metric':return `<div class="iu-metric"><span>${inline(d.label||'',sources)}</span><strong>${inline(d.value||'',sources)}</strong>${d.detail?`<small>${inline(d.detail,sources)}</small>`:''}</div>`;case'progress':{const v=Math.max(0,Math.min(100,Number(d.value)||0));return `<div class="iu-progress"><div><span>${inline(d.label||'',sources)}</span><strong>${v}%</strong></div><div class="iu-progress-track"><i style="width:${v}%"></i></div></div>`}case'chart':return chart(d);case'code':return `<pre><code data-language="${esc(d.language||'')}">${esc(d.code||'')}</code></pre>`;default:return''}}
  function structured(response,fallback,sources){const comps=Array.isArray(response?.components)?response.components:[];if(comps.length)return comps.map(c=>component(c,sources)).join('');return rich(fallback,sources)}
  function table(lines,sources){const rows=lines.map(l=>l.trim().replace(/^\||\|$/g,'').split('|').map(x=>x.trim()));return tableData({headers:rows[0]||[],rows:rows.slice(2)},sources)}
  function rich(raw,sources=[]){const placeholders=[];raw=String(raw||'').replace(/```wae-chart\s*([\s\S]*?)```/gi,(_,body)=>{let html='';try{html=chart(JSON.parse(body.trim()))}catch{}const key=`@@BLOCK${placeholders.length}@@`;placeholders.push(html||`<pre><code>${esc(body)}</code></pre>`);return key}).replace(/```(?:\w+)?\s*([\s\S]*?)```/g,(_,body)=>{const key=`@@BLOCK${placeholders.length}@@`;placeholders.push(`<pre><code>${esc(body.trim())}</code></pre>`);return key});const lines=raw.split('\n'),out=[];let i=0;while(i<lines.length){const line=lines[i],trim=line.trim();if(!trim){i++;continue}if(/^@@BLOCK\d+@@$/.test(trim)){out.push(trim);i++;continue}const metric=trim.match(/^:::metric\s+([^|]+)\|([^|]+)(?:\|(.+))?$/i);if(metric){out.push(component({type:'metric',data:{label:metric[1].trim(),value:metric[2].trim(),detail:metric[3]?.trim()}},sources));i++;continue}const prog=trim.match(/^:::progress\s+([^|]+)\|(\d+(?:\.\d+)?)$/i);if(prog){out.push(component({type:'progress',data:{label:prog[1].trim(),value:Number(prog[2])}},sources));i++;continue}if(/^\|?.+\|.+\|?$/.test(trim)&&i+1<lines.length&&/^\s*\|?\s*:?-{3,}/.test(lines[i+1])){const block=[line,lines[i+1]];i+=2;while(i<lines.length&&/\|/.test(lines[i])&&lines[i].trim())block.push(lines[i++]);out.push(table(block,sources));continue}const h=trim.match(/^(#{1,4})\s+(.+)$/);if(h){const level=Math.min(4,h[1].length+1);out.push(`<h${level}>${inline(h[2],sources)}</h${level}>`);i++;continue}if(/^>\s?/.test(trim)){out.push(`<blockquote>${inline(trim.replace(/^>\s?/,''),sources)}</blockquote>`);i++;continue}if(/^[-*+]\s+/.test(trim)){const list=[];while(i<lines.length&&/^\s*[-*+]\s+/.test(lines[i]))list.push(lines[i++].replace(/^\s*[-*+]\s+/,''));out.push(`<ul>${list.map(x=>`<li>${inline(x,sources)}</li>`).join('')}</ul>`);continue}if(/^\d+[.)]\s+/.test(trim)){const list=[];while(i<lines.length&&/^\s*\d+[.)]\s+/.test(lines[i]))list.push(lines[i++].replace(/^\s*\d+[.)]\s+/,''));out.push(`<ol>${list.map(x=>`<li>${inline(x,sources)}</li>`).join('')}</ol>`);continue}const para=[trim];i++;while(i<lines.length&&lines[i].trim()&&!/^(#{1,4})\s+|^:::|^[-*+]\s+|^\d+[.)]\s+|^>\s?|^@@BLOCK\d+@@$/.test(lines[i].trim())){if(i+1<lines.length&&/\|/.test(lines[i])&&/^\s*\|?\s*:?-{3,}/.test(lines[i+1]))break;para.push(lines[i].trim());i++}out.push(`<p>${inline(para.join(' '),sources)}</p>`)}let html=out.join('');placeholders.forEach((v,idx)=>{html=html.replace(`@@BLOCK${idx}@@`,v)});return html}
  function sourcesHtml(sources){if(!Array.isArray(sources)||!sources.length)return'';return `<section class="iu-sources"><h4>Fuentes verificadas</h4><div>${sources.slice(0,8).map(s=>`<a id="source-${esc(s.key||'')}" href="${esc(safeUrl(s.url))}" target="_blank" rel="noopener noreferrer"><span>${esc(s.key||'Fuente')}</span><strong>${esc(s.title||s.host||'Fuente')}</strong><small>${esc(s.host||'')}</small></a>`).join('')}</div></section>`}
  async function feedback(messageId,conversationId,rating,button){const sid=localStorage.getItem('iu.sessionId')||'',secret=localStorage.getItem('iu.sessionSecret')||'';if(!sid||!secret){window.toast?.('La sesión aún se está sincronizando');return}button.disabled=true;try{const r=await fetch(RUNTIME_ENDPOINT,{method:'POST',headers:{'content-type':'application/json','apikey':SUPABASE_KEY,'x-client-info':'wae-feedback/1.0'},body:JSON.stringify({action:'feedback',session_id:sid,session_secret:secret,message_id:messageId||null,conversation_id:conversationId||localStorage.getItem('iu.conversationId')||null,rating}),cache:'no-store'});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.error||`feedback_${r.status}`);button.parentElement?.querySelectorAll('[data-feedback]').forEach(b=>b.classList.remove('selected'));button.classList.add('selected');window.toast?.(rating>0?'Respuesta añadida al dataset de mejora':'Feedback registrado para corrección')}catch(e){console.warn(e);window.toast?.('No pude registrar el feedback')}finally{button.disabled=false}}
  function workspace(html){const editor=document.querySelector('#documentEditor');if(!editor)return;editor.querySelector('.placeholder-line')?.remove();editor.insertAdjacentHTML('beforeend',`<section class="workspace-ai-block">${html}</section>`);document.querySelector('#workspaceBtn')?.click();window.toast?.('Respuesta enviada al Workspace')}
  function metaHtml(response,runtime){const m=response?.metadata||{},parts=[];if(runtime?.model||m.model)parts.push(runtime?.model||m.model);const latency=runtime?.latency_ms??m.latencyMs;if(Number.isFinite(Number(latency))&&Number(latency)>0)parts.push(`${Number(latency)} ms`);if((runtime?.web_sources||response?.sources||[]).length)parts.push(`${(runtime?.web_sources||response?.sources||[]).length} fuentes`);if(Number(runtime?.memory_count??m.memoryCount)>0)parts.push(`${Number(runtime?.memory_count??m.memoryCount)} memorias`);return parts.length?`<div class="iu-answer-meta">${parts.map(x=>`<span>${esc(x)}</span>`).join('')}</div>`:''}
  function enhance(article){if(!article||article.dataset.premium==='true'||article.id==='typingMessage'||!article.classList.contains('assistant'))return;const legacy=article.querySelector(':scope > .rich-answer'),plain=article.querySelector(':scope > p');if(!legacy&&!plain)return;const runtime=window.__iuLastRuntime||{},assistants=[...document.querySelectorAll('#messages .message.assistant:not(#typingMessage)')],isLast=assistants.at(-1)===article,liveRaw=String(runtime.reply||'').trim();const raw=isLast&&liveRaw?liveRaw:String((legacy||plain)?.textContent||'').trim();if(!raw)return;const isLatest=Boolean(isLast&&liveRaw),response=isLatest&&runtime.response?.schema==='assistant-response/v1'?runtime.response:null,sources=isLatest?(response?.sources||runtime.web_sources||[]):[],speechText=isLatest?(response?.speechText||runtime.speech_text||raw):raw,messageId=isLatest?(runtime.message_id||response?.metadata?.messageId||null):null,conversationId=isLatest?(runtime.conversation_id||response?.metadata?.conversationId||null):null;article.querySelector(':scope > .answer-actions')?.remove();const html=structured(response,raw,sources);if(legacy){legacy.classList.add('rich-content');legacy.innerHTML=html;if(sources.length)legacy.insertAdjacentHTML('afterend',sourcesHtml(sources))}else plain.outerHTML=`<div class="rich-content">${html}</div>${sourcesHtml(sources)}`;if(isLatest){article.insertAdjacentHTML('beforeend',metaHtml(response,runtime));if(messageId)article.dataset.messageId=messageId}const actions=document.createElement('div');actions.className='iu-answer-actions';actions.innerHTML='<button type="button" data-act="listen">Escuchar</button><button type="button" data-act="copy">Copiar</button><button type="button" data-act="workspace">Workspace</button><span></span><button type="button" data-feedback="1" aria-label="Útil">Útil</button><button type="button" data-feedback="-1" aria-label="Mejorar">Mejorar</button>';article.appendChild(actions);actions.querySelector('[data-act="listen"]').addEventListener('click',e=>{if(document.documentElement.dataset.voicePlaying==='true'){window.__waeVoice?.stop();e.currentTarget.textContent='Escuchar'}else{e.currentTarget.textContent='Detener';window.__waeVoice?.speak(speechText,{force:true}).finally(()=>e.currentTarget.textContent='Escuchar')}});actions.querySelector('[data-act="copy"]').addEventListener('click',()=>navigator.clipboard?.writeText(raw).then(()=>window.toast?.('Respuesta copiada')).catch(()=>{}));actions.querySelector('[data-act="workspace"]').addEventListener('click',()=>workspace(article.querySelector('.rich-content')?.innerHTML||esc(raw)));actions.querySelectorAll('[data-feedback]').forEach(b=>b.addEventListener('click',()=>feedback(messageId,conversationId,Number(b.dataset.feedback),b)));article.dataset.premium='true';if(isLatest&&window.__waeVoice?.enabled&&!article.dataset.autoSpoken){article.dataset.autoSpoken='true';setTimeout(()=>window.__waeVoice.speak(speechText).catch(()=>{}),120)}}
  function enhanceAll(){document.querySelectorAll('#messages .message.assistant').forEach(enhance)}
  function init(){const messages=document.querySelector('#messages');if(messages){enhanceAll();new MutationObserver(()=>enhanceAll()).observe(messages,{childList:true,subtree:true})}const form=document.querySelector('#composer');form?.addEventListener('submit',()=>window.__waeVoice?.unlock(),true);const voiceBtn=document.querySelector('#voiceBtn');if(voiceBtn){voiceBtn.title='Voz automática';voiceBtn.setAttribute('aria-pressed',String(window.__waeVoice?.enabled!==false));voiceBtn.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();window.__waeVoice?.toggle().then(v=>voiceBtn.setAttribute('aria-pressed',String(v)))},true)}}
  document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init();window.__waeRich={render:rich,structured};
})();
