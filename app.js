const $=(s,r=document)=>r.querySelector(s);const $$=(s,r=document)=>[...r.querySelectorAll(s)];
const state={mode:localStorage.getItem('wae.mode')||'general',endpoint:localStorage.getItem('wae.endpoint')||'',coreName:localStorage.getItem('wae.coreName')||'Universal Core',document:localStorage.getItem('wae.document')||'',html:localStorage.getItem('wae.html')||'',messages:JSON.parse(localStorage.getItem('wae.messages')||'[]')};
const modeLabels={general:'General',research:'Investigar',code:'Programar',analysis:'Analizar',design:'Diseñar'};
function safeText(t){return String(t??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function nowLabel(){return new Intl.DateTimeFormat('es-MX',{hour:'2-digit',minute:'2-digit'}).format(new Date())}function persistMessages(){localStorage.setItem('wae.messages',JSON.stringify(state.messages.slice(-60)))}
function renderMessages(){const t=$('#messages');t.innerHTML='';if(!state.messages.length){state.messages=[{role:'assistant',text:'Sistema listo. Puedo convertir una instrucción en investigación, arquitectura, código, análisis o diseño. Elige una capacidad o escribe la misión directamente.',at:nowLabel()}];persistMessages()}state.messages.forEach(renderMessage);scrollChat()}
function renderMessage(m){const e=document.createElement('article');e.className=`message ${m.role==='user'?'user':'assistant'}`;const n=m.role==='user'?'Tú':state.coreName;e.innerHTML=`<div class="message-meta"><strong>${safeText(n)}</strong><span>${safeText(m.at||nowLabel())}</span></div><p>${safeText(m.text)}</p>`;$('#messages').appendChild(e)}
function showTyping(){const e=document.createElement('article');e.className='message assistant';e.id='typingMessage';e.innerHTML=`<div class="message-meta"><strong>${safeText(state.coreName)}</strong><span>procesando</span></div><span class="typing"><i></i><i></i><i></i></span>`;$('#messages').appendChild(e);scrollChat()}function hideTyping(){$('#typingMessage')?.remove()}function scrollChat(){requestAnimationFrame(()=>{const s=$('.chat-layout');s.scrollTop=s.scrollHeight})}function addMessage(role,text){const i={role,text,at:nowLabel()};state.messages.push(i);persistMessages();renderMessage(i);scrollChat()}
function localReply(message,mode){const i={research:'Misión de investigación recibida.',code:'Misión de ingeniería recibida.',analysis:'Misión de análisis recibida.',design:'Misión de diseño recibida.',general:'Misión recibida.'};const b={research:'Voy a estructurar hipótesis, datos necesarios, fuentes y una síntesis accionable. Conecta un endpoint IA en Configuración para ejecutar investigación con un modelo real.',code:'La ruta correcta es: requisitos → arquitectura → implementación → pruebas → corrección → entrega. El workspace está listo para convertir la respuesta en documento o HTML.',analysis:'Separaré hechos, supuestos, riesgos, métricas y decisión recomendada. Conecta tu backend para sustituir este motor local por inferencia real.',design:'Traduciré el objetivo a flujo, jerarquía, componentes, estados y criterios de calidad. Puedes abrir Workspace para trabajar el entregable.',general:'Puedo enrutar esta solicitud hacia investigación, programación, análisis o diseño. Para respuestas reales del modelo, configura tu endpoint IA.'};return `${i[mode]||i.general}\n\n${b[mode]||b.general}\n\nEntrada: “${message.slice(0,180)}${message.length>180?'…':''}”`}
async function getAIReply(message){if(!state.endpoint)return localReply(message,state.mode);const c=new AbortController(),timer=setTimeout(()=>c.abort(),45000);try{const r=await fetch(state.endpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message,mode:state.mode}),signal:c.signal});if(!r.ok)throw new Error(`HTTP ${r.status}`);const d=await r.json();if(!d||typeof d.reply!=='string')throw new Error('Respuesta inválida');return d.reply}catch(e){return `No pude usar el endpoint configurado (${e.message}). Mantengo la sesión activa con el motor local.\n\n${localReply(message,state.mode)}`}finally{clearTimeout(timer)}}
function setMode(mode){state.mode=mode;localStorage.setItem('wae.mode',mode);$('#modePill').textContent=modeLabels[mode]||'General';$$('.capability-card').forEach(b=>b.classList.toggle('active',b.dataset.mode===mode));$('#messageInput').focus()}function resetConversation(){state.messages=[];persistMessages();renderMessages();toast('Nueva conversación creada')}function openDrawer(){$('#drawer').classList.add('open');$('#drawer').setAttribute('aria-hidden','false');$('#scrim').classList.add('visible')}function closeDrawer(){$('#drawer').classList.remove('open');$('#drawer').setAttribute('aria-hidden','true');$('#scrim').classList.remove('visible')}function openWorkspace(){$('#workspace').classList.add('open');$('#workspace').setAttribute('aria-hidden','false');closeDrawer()}function closeWorkspace(){$('#workspace').classList.remove('open');$('#workspace').setAttribute('aria-hidden','true')}
function switchWorkspaceTab(tab){$$('.workspace-tabs button').forEach(b=>b.classList.toggle('active',b.dataset.tab===tab));$$('.tab-panel').forEach(p=>p.classList.toggle('active',p.id===`panel-${tab}`));if(tab==='html')updatePreview()}function markDirty(){$('#saveState').textContent='Cambios sin guardar'}function saveWorkspace(){state.document=$('#documentEditor').innerHTML;state.html=$('#htmlEditor').value;localStorage.setItem('wae.document',state.document);localStorage.setItem('wae.html',state.html);$('#saveState').textContent='Guardado';toast('Workspace guardado')}function updatePreview(){$('#htmlPreview').srcdoc=$('#htmlEditor').value}
function downloadText(filename,content,type='text/plain'){const b=new Blob([content],{type}),u=URL.createObjectURL(b),a=document.createElement('a');a.href=u;a.download=filename;a.click();setTimeout(()=>URL.revokeObjectURL(u),1000)}function exportWorkspace(){const a=$('.workspace-tabs button.active')?.dataset.tab;if(a==='html')downloadText('wae-canvas.html',$('#htmlEditor').value,'text/html');else downloadText('wae-workspace.html',`<!doctype html><meta charset="utf-8"><title>WAE Workspace</title><body>${$('#documentEditor').innerHTML}</body>`,'text/html');toast('Archivo exportado')}
let toastTimer;function toast(message){let e=$('.toast');if(!e){e=document.createElement('div');e.className='toast';$('.app-shell').appendChild(e)}e.textContent=message;e.classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>e.classList.remove('show'),1800)}function autosizeInput(){const i=$('#messageInput');i.style.height='auto';i.style.height=`${Math.min(i.scrollHeight,150)}px`}async function submitMessage(ev){ev.preventDefault();const i=$('#messageInput'),m=i.value.trim();if(!m)return;i.value='';autosizeInput();addMessage('user',m);showTyping();const r=await getAIReply(m);hideTyping();addMessage('assistant',r)}
function openSettings(){$('#apiEndpoint').value=state.endpoint;$('#coreName').value=state.coreName;$('#settingsDialog').showModal();closeDrawer()}function saveSettings(){state.endpoint=$('#apiEndpoint').value.trim();state.coreName=$('#coreName').value.trim()||'Universal Core';localStorage.setItem('wae.endpoint',state.endpoint);localStorage.setItem('wae.coreName',state.coreName);$('#settingsDialog').close();renderMessages();toast('Configuración guardada')}function initDocument(){if(state.document)$('#documentEditor').innerHTML=state.document;if(state.html)$('#htmlEditor').value=state.html;updatePreview()}
function initInteractions(){$('#menuBtn').addEventListener('click',openDrawer);$('#closeDrawerBtn').addEventListener('click',closeDrawer);$('#scrim').addEventListener('click',closeDrawer);$('#newChatBtn').addEventListener('click',resetConversation);$('#drawerNewChat').addEventListener('click',()=>{resetConversation();closeDrawer()});$('#workspaceBtn').addEventListener('click',openWorkspace);$('#drawerWorkspace').addEventListener('click',openWorkspace);$('#closeWorkspaceBtn').addEventListener('click',closeWorkspace);$('#composer').addEventListener('submit',submitMessage);$('#messageInput').addEventListener('input',autosizeInput);$('#messageInput').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();$('#composer').requestSubmit()}});$$('.capability-card').forEach(b=>b.addEventListener('click',()=>setMode(b.dataset.mode)));$$('.workspace-tabs button').forEach(b=>b.addEventListener('click',()=>switchWorkspaceTab(b.dataset.tab)));$$('.toolbar button').forEach(b=>b.addEventListener('click',()=>{document.execCommand(b.dataset.format,false,b.dataset.value||null);$('#documentEditor').focus();markDirty()}));$('#documentEditor').addEventListener('input',markDirty);$('#htmlEditor').addEventListener('input',()=>{updatePreview();markDirty()});$('#saveBtn').addEventListener('click',saveWorkspace);$('#exportBtn').addEventListener('click',exportWorkspace);$('#attachBtn').addEventListener('click',()=>$('#fileInput').click());$('#fileInput').addEventListener('change',e=>{const c=e.target.files.length;if(c)toast(`${c} archivo${c>1?'s':''} seleccionado${c>1?'s':''}`)});$('#voiceBtn').addEventListener('click',()=>toast('Voz lista para conectar a STT/TTS'));$('#settingsBtn').addEventListener('click',openSettings);$('#saveSettingsBtn').addEventListener('click',saveSettings);$$('.nav-list button[data-view]').forEach(b=>b.addEventListener('click',()=>{toast(`${b.querySelector('span').textContent}: módulo preparado`);closeDrawer()}));window.addEventListener('keydown',e=>{if(e.key==='Escape'&&$('#workspace').classList.contains('open'))closeWorkspace()})}
function registerSW(){if('serviceWorker'in navigator&&location.protocol!=='file:')navigator.serviceWorker.register('./sw.js').catch(()=>{})}renderMessages();initDocument();initInteractions();setMode(state.mode);registerSW();
